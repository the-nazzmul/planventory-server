export const addMoney = (a: number, b: number): number => a + b;

export const subtractMoney = (a: number, b: number): number => a - b;

export const multiplyMoney = (amount: number, quantity: number): number => amount * quantity;
